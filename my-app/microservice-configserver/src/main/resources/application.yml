server:
  port: 8888
spring:
  profiles:
    active: native
  application:
    name: config-serv
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configuration
